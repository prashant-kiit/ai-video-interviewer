root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  entrypoint = "./tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]

[run]
  cmd = "./tmp/server"
